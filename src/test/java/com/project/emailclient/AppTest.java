package com.project.emailclient;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Smoke test — replaced the old JUnit 3 placeholder.
 * Real coverage is in the focused test classes.
 */
class AppTest {

    @Test
    void smokeTest() {
        assertTrue(true, "Project compiles and JUnit 5 is wired up correctly");
    }
}
